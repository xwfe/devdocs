# DevDocs Rust 依赖对照表

## 概述

本文档详细记录了从原版 Ruby DevDocs 到 Rust 实现的依赖库对应关系，包括功能映射、性能对比和迁移建议。

## 核心依赖映射

### HTTP 客户端

| 原版 Ruby | 当前 Rust | 功能对应 | Star 数 | 最近更新 | 状态 |
|-----------|-----------|----------|---------|----------|------|
| **Typhoeus** | **reqwest** | HTTP 客户端 | 8.9k | 2024 | ✅ 完全对应 |
| - 并发请求 | - tokio 异步 | 异步处理 | - | - | ✅ 更优 |
| - libcurl 后端 | - hyper 后端 | 底层实现 | - | - | ✅ 原生 Rust |
| - 连接池 | - 连接池 | 连接复用 | - | - | ✅ 对应 |

### HTML 解析

| 原版 Ruby | 当前 Rust | 功能对应 | Star 数 | 最近更新 | 状态 |
|-----------|-----------|----------|---------|----------|------|
| **Nokogiri** | **scraper** | HTML 解析 | 18.1k → 1.8k | 2024 | ✅ 功能对应 |
| - CSS 选择器 | - CSS 选择器 | 元素选择 | - | - | ✅ 完全对应 |
| - XPath 支持 | - ❌ 无 XPath | XPath 查询 | - | - | ⚠️ 功能缺失 |
| - DOM 操作 | - ElementRef | DOM 访问 | - | - | ✅ 只读访问 |

**补充库建议**：
```toml
# 添加 XPath 支持
xpath_reader = "0.4"  # 2k stars, 2023 更新
```

### JSON 处理

| 原版 Ruby | 当前 Rust | 功能对应 | Star 数 | 最近更新 | 状态 |
|-----------|-----------|----------|---------|----------|------|
| **JSON** (内置) | **serde_json** | JSON 序列化 | - → 7.8k | 2024 | ✅ 更强类型安全 |
| - 动态解析 | - 类型化解析 | 解析方式 | - | - | ✅ 更安全 |
| - 运行时错误 | - 编译时检查 | 错误处理 | - | - | ✅ 更早发现错误 |

### Web 框架

| 原版 Ruby | 当前 Rust | 功能对应 | Star 数 | 最近更新 | 状态 |
|-----------|-----------|----------|---------|----------|------|
| **Rails/Sinatra** | **axum** | Web 服务器 | 45.6k → 17.2k | 2024 | ✅ 现代化架构 |
| - 中间件 | - 中间件 | 请求处理 | - | - | ✅ 更类型安全 |
| - 路由 | - 路由 | URL 路由 | - | - | ✅ 编译时验证 |
| - 模板引擎 | - ❌ 暂无 | 模板渲染 | - | - | ⚠️ 需要添加 |

**建议添加**：
```toml
# 模板引擎
tera = "1.19"  # 2.7k stars, 2024 更新
handlebars = "4.4"  # 1.2k stars, 2024 更新
```

### 工具库 (ActiveSupport 替代)

| 原版 Ruby | 当前 Rust | 功能对应 | Star 数 | 最近更新 | 状态 |
|-----------|-----------|----------|---------|----------|------|
| **ActiveSupport** | **多个 crate** | 工具函数集 | 15.2k → 分散 | - | ✅ 模块化设计 |

#### 字符串处理
```toml
# 字符串操作
regex = "1.10"           # 3.2k stars, 2024
inflector = "0.11"       # 170 stars, 2021 (需要更新)
```

**建议替换**：
```toml
# 更好的字符串处理库
heck = "0.4"             # 1.5k stars, 2024 - 命名转换
```

#### 时间处理
```toml
# 时间和日期
chrono = "0.4"           # 2.8k stars, 2024
time = "0.3"             # 1.1k stars, 2024 (备选)
```

#### 文件系统
```toml
# 文件系统操作
walkdir = "2.4"          # 1.0k stars, 2024
globset = "0.4"          # 950 stars, 2024
```

## 新增 Rust 特有依赖

### 异步运行时

| Crate | 功能 | Star 数 | 更新状态 | 说明 |
|-------|------|---------|----------|------|
| **tokio** | 异步运行时 | 24.8k | 2024 | Ruby 无对应，Rust 特有 |
| **futures** | Future 工具 | 5.1k | 2024 | 异步编程基础 |

### 错误处理

| Crate | 功能 | Star 数 | 更新状态 | 说明 |
|-------|------|---------|----------|------|
| **thiserror** | 错误定义 | 3.8k | 2024 | 结构化错误处理 |
| **anyhow** | 错误传播 | 4.9k | 2024 | 简化错误处理 |

### 命令行界面

| Crate | 功能 | Star 数 | 更新状态 | Ruby 对应 |
|-------|------|---------|----------|-----------|
| **clap** | CLI 解析 | 13.3k | 2024 | OptionParser, Thor |

## 优化建议依赖

### 性能优化

```toml
# 更快的 JSON 处理
simd-json = "0.13"       # 1.1k stars, 2024 - SIMD 加速

# 更快的字符串操作
ahash = "0.8"            # 1.3k stars, 2024 - 更快的哈希

# 内存优化
compact_str = "0.7"      # 320 stars, 2024 - 紧凑字符串
```

### 开发体验

```toml
# 日志系统
tracing = "0.1"          # 4.5k stars, 2024 - 结构化日志
env_logger = "0.10"      # 850 stars, 2024 - 简单日志

# 配置管理
config = "0.13"          # 2.2k stars, 2024 - 配置文件
```

## 功能增强依赖

### 缺失功能补充

```toml
# XPath 支持 (补充 scraper)
xpath_reader = "0.4"     # 170 stars, 2023
roxmltree = "0.18"       # 590 stars, 2024 - XML 解析

# 模板引擎
tera = "1.19"            # 2.7k stars, 2024 - Jinja2 风格
handlebars = "4.4"       # 1.2k stars, 2024 - Handlebars 兼容

# 压缩支持
flate2 = "1.0"           # 1.8k stars, 2024 - gzip/deflate
tar = "0.4"              # 600 stars, 2024 - tar 档案
```

### 数据库支持 (如需要)

```toml
# SQLite (文档缓存)
rusqlite = "0.29"        # 2.8k stars, 2024
sled = "0.34"            # 7.8k stars, 2023 - 嵌入式数据库
```

## 依赖统计对比

### 原版 Ruby 依赖
- **总数**: ~15 个主要 gem
- **总下载量**: > 1B 下载
- **维护状态**: 混合（部分库更新缓慢）

### 当前 Rust 依赖
- **总数**: ~25 个 crate
- **总 star 数**: > 100k
- **维护状态**: 活跃（90%+ 在 2024 年更新）

### 优势对比

| 方面 | Ruby 原版 | Rust 实现 | 优势 |
|------|-----------|-----------|------|
| **类型安全** | 运行时检查 | 编译时检查 | Rust ✅ |
| **性能** | 解释执行 | 原生编译 | Rust ✅ |
| **内存安全** | GC 管理 | 所有权系统 | Rust ✅ |
| **生态成熟度** | 非常成熟 | 快速发展 | Ruby ✅ |
| **学习曲线** | 较平缓 | 较陡峭 | Ruby ✅ |

## 迁移路线图

### 阶段 1: 核心功能对齐 (已完成)
- [x] HTTP 客户端 (reqwest)
- [x] HTML 解析 (scraper)
- [x] JSON 处理 (serde_json)
- [x] 异步运行时 (tokio)

### 阶段 2: 功能增强 (进行中)
- [ ] XPath 支持
- [ ] 模板引擎集成
- [ ] 压缩支持
- [ ] 配置管理

### 阶段 3: 性能优化 (计划中)
- [ ] SIMD JSON 处理
- [ ] 内存优化
- [ ] 并发优化
- [ ] 缓存机制

## 建议的 Cargo.toml 更新

```toml
[dependencies]
# 现有核心依赖 (保持)
reqwest = { version = "0.11", features = ["json", "gzip"] }
scraper = "0.18"
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
axum = "0.7"

# 新增功能增强
xpath_reader = "0.4"     # XPath 支持
tera = "1.19"            # 模板引擎
heck = "0.4"             # 字符串转换
config = "0.13"          # 配置管理
tracing = "0.1"          # 结构化日志

# 性能优化 (可选)
simd-json = { version = "0.13", optional = true }
ahash = "0.8"
compact_str = "0.7"

[features]
default = []
simd = ["simd-json"]     # SIMD 加速功能
```

## 总结

Rust 实现在保持原版功能的基础上，通过现代化的依赖选择获得了：

1. **更好的类型安全**：编译时错误检查
2. **更高的性能**：原生编译 + SIMD 优化
3. **更好的并发**：async/await + tokio 生态
4. **更好的内存管理**：零成本抽象 + 所有权系统

同时通过模块化的 crate 生态，避免了大而全的依赖库，实现了更精确的依赖管理。